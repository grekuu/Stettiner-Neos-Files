prototype(Stettiner.Test:Content.Headline) < prototype(Neos.Neos:ContentComponent) {
    title = Neos.Neos:Editable {
        property = 'title'
    }

    renderer = afx`
        <div class="headline content">{props.title}</div>
    `
}
